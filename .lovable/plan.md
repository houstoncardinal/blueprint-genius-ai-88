Update the homepage hero headline from "From one spark to a launch-ready blueprint" to the user's selected option C: **"Describe it. Architect it. Ship it."**

**Scope:**
- Change the `<h1>` headline text in `src/routes/index.tsx`.
- Keep the existing animation structure and styling intact (per-word animation classes, gradient accent, underline glow).

**No other copy changes unless requested.**